# SMTP Configuration for Contact Form
# These credentials are used to send emails from the contact form
SMTP_HOST=your-smtp-host.com
SMTP_PORT=465
SMTP_USER=your-email@example.com
SMTP_PASS=your-smtp-password

# Email address that will receive contact form submissions
TO_ADDRESS=jaishukreddy7@gmail.com

# Optional: Comma-separated list of allowed origins for CORS
# If not set, defaults to localhost origins
# CLIENT_ORIGIN=http://localhost:3000,https://yourdomain.com

# Optional: Custom contact API endpoint (defaults to /api/contact)
# VITE_CONTACT_ENDPOINT=/api/contact
